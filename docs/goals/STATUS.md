# Goal Status

상태와 검증 Evidence의 유일한 기록 장소다. `VERIFIED`는 실제 실행 결과가 있을 때만 사용한다. 역할과 lane은 초기안이며 변경 시 [`../team/`](../team/) 문서와 함께 갱신한다.

## Active lanes

| Lane | Active Goal | Owner | 상태 |
|---|---|---|---|
| Core | S4-G4 | 본인/팀 리드 | IMPLEMENTED — active Play Field clip의 실제 Web release 증거 대기 |
| Presentation | 없음 | 팀원 A | available — S5-G6 producer and S5-G6I Main wiring are VERIFIED |
| Content/Systems/Release | 없음 | Content/Systems/Release 담당 | available |
| Integration | 없음 | 본인/팀 리드 | available — S7-G1 Blizzard spawn consumer wired; CUT-IN producer still pending |

각 lane은 `IN PROGRESS`를 최대 하나만 가진다. 서로 다른 lane은 Dependencies와 Integration Point가 충족되면 병렬 진행할 수 있다.

## Integration lock

| Integration Goal | Locked Files | 상태 |
|---|---|---|
| 없음 | — | released |

## Goal matrix

| Goal | Owner | 상태 | Evidence / 남은 검증 |
|---|---|---|---|
| S6-G6 Minimal Final Settlement presentation | Presentation | IMPLEMENTED | 2026-08-19 단일 draw node가 100공 snapshot을 최대 64 visual sample로 제한하고 0.5초 pixel dissolve·Stage Score count-up 뒤 완료 signal을 1회 발행한다. Godot 4.7.1 CLI `S6_G6_VERIFIED samples=64 duration=0.5 score_countup=true completion=1 core_readonly=true`; S6-G1/S3-G6 회귀 통과, Main smoke runtime error 0. Native Main에서 F7 확인 경로를 실행했다. 최신 Clear 확인 계약에서도 visual 완료 signal은 Core Settlement/CLEARED를 막지 않는 non-gating이며 S6-G6I는 `RETIRED`다. |
| S6-G6I Final Settlement handoff wiring | Integration | RETIRED | 최신 S5-G6I 계약은 Core `Final Settlement → CLEARED`를 Presentation 완료로 지연하지 않고, 확인은 `CLEARED→SHIFTING` 경계에서만 기다린다. S6-G6의 `final_settlement_presentation_finished()`는 score/state를 바꾸지 않는 read-only visual completion으로 유지한다. |
| S0-G1 프로젝트 부트 | Integration | VERIFIED | 2026-08-09 Godot 4.7.1 CLI headless run exit 0. Primary `godot` MCP runtime launch은 strict elicitation 미지원으로 차단; baseline 통과 후 fallback `godot_fallback` debug run 성공(치명 runtime error 없음). WASAPI 출력 장치 실패로 dummy audio fallback만 기록됨. |
| S0-G2 입력·공유 씬 골격 | Integration | VERIFIED | Input Map 6개 action과 공유 mount tree 확인. Godot 4.7.1 CLI editor load/Main run exit 0, `godot` MCP scene tree 조회 성공. 실제 A/D+회전 동시 동작 검증은 입력 소비자가 존재하는 S1-G2로 이동. |
| S0-G3 Web smoke | Content/Systems | VERIFIED | Godot 4.7.1 single-threaded Web release export exit 0. 로컬 HTTP에서 HTML/JS/WASM/PCK 모두 200. 새 브라우저 탭에서 Godot 4.7.1 WebGL2 기동, Canvas 1280×720→1024×768 resize, Canvas focus 유지 및 A/← 입력 전달, console warning/error 0건. |
| S1-G1 배열 풀 낙하 | Core | VERIFIED | 2026-08-10 팀 플레이테스트로 승인된 Lv1 radius 4 logical units(visual/collision diameter 8)와 160 world units/s Spawn tuning을 gravity 0 free-flight 경계에서 재검증. 100공/slot reuse/좌·우·상단 반사·열린 하단 구조 유지, Godot 4.7.1 headless exit 0 및 Primary `godot` runtime 확인. |
| S1-G2 패들 조작·반사 | Core | VERIFIED | 2026-08-11 direct Mouse sweep 중 큰 공이 깊게 겹칠 때, sweep entry normal을 유지한 채 최종 Paddle transform 기준으로 surface 밖 보정하도록 회귀 수정했다. 2026-08-21 빠른 Mouse 왕복 중 이전 contact lock이 남아 공 sweep을 건너뛰던 회귀를 수정했다. 접촉 접근 판정과 lock 재개방은 실제 transform velocity를 사용하고, 반사에 전달하는 impact만 기존 900 cap을 적용한다. Primary `godot` reflection test exit 0(양면·translation·rotation·large overlap·capped-speed Mouse recontact), mouse test exit 0, S1-G1 verification exit 0, S1-G3 verification exit 0, Main runtime에서 빠른 Mouse 왕복 입력과 runtime error 0을 확인했다. Web은 기존 사용자 Chrome 수동 검증을 유지한다. |
| S1-G3 Active Cashout 논리 | Core | VERIFIED | 2026-08-10 Lv1 radius 4에서도 열린 하단 Cashout, slot reuse, stage/run ledger 1회 반영과 reset을 재검증. Godot 4.7.1 headless exit 0 및 Primary `godot` runtime 확인. |
| S1-G4 최소 HUD | Presentation | VERIFIED | `score_changed`/`ball_count_changed` read-only 구독과 `reset_view()` 구현. Godot 4.7.1 자동검증 exit 0: stage/run 1회 표시, balls 1 표시, HUD reset 후 Core state 불변. Primary `godot` validate 4/4. |
| S1-G5 Pause·Restart 요청 UI | Content/Systems | VERIFIED | `pause_requested`/`retry_requested` request-only UI와 paused label 상태 구현. Godot 4.7.1 자동검증 exit 0: input pause 1회, button retry 1회, SceneTree/gameplay 직접 변경 없음. Primary `godot` validate 4/4. |
| S1-G6 Pause·Restart 통합 | Integration | VERIFIED | 2026-08-09 최신 clean Web release export를 사용자의 실제 Chrome 수동 플레이로 검증. Canvas, Mouse X direct 이동, Mouse Wheel 자유회전, A/D 이동, A/D release 뒤 위치 유지, 다음 실제 MouseMotion에서만 Mouse control 복귀, Paddle 이동/회전 충돌, Pause/Retry가 정상이며 browser console game error 0. |
| S2-G1 공·점수 데이터 | Content/Systems | VERIFIED | 2026-08-12 catalog resource와 content verification을 최신 계약으로 재정렬했다: Lv6 Sun, Lv7 Red Giant, Lv9 Nebula, Lv10 Galaxy, Lv14 `Black Hole`/`black_hole`. 15종의 score/radius/mass/fx tier와 Time Bonus 부재를 유지했다. Primary `godot` validate로 definition/catalog/test script/test scene 4/4 valid, Main runtime에서 15개 전부의 name/key/score를 직접 조회했고 runtime error 0. 이동 Black Hole 맵 기믹은 BallDefinition과 별도다. Runtime visual/collision 크기는 현재 Stage local level 계약을 따른다. |
| S2-G2 Merge·non-Merge contact 후보 탐색 | Core | VERIFIED | 2026-08-21 same-level Merge grid는 유지하고, 서로 다른 level과 더 성장할 수 없는 최고공 pair를 모든 level bucket의 swept 인접 cell에서 non-Merge contact 후보로 찾도록 확장했다. Godot 4.7.1 headless fixture에 오류가 없었고 Primary `godot` validation 5/5를 통과했다. |
| S2-G3 결정적 Merge·non-Merge contact commit | Core | VERIFIED | 2026-08-21 성장 가능한 same-level Merge 우선순위는 유지한다. 서로 다른 level과 더 성장할 수 없는 최고공 pair는 swept relative circle contact, mass/current velocity 반사, penetration correction으로 commit하며 공을 소비하지 않는다. Godot 4.7.1 headless fixture에 오류가 없었고 Primary `godot` validation 5/5를 통과했다. |
| S2-G4 Score formatter | Content/Systems | VERIFIED | 2026-08-10 `format_score(value)` pure API로 0, K/M/B/T 경계, 반올림 승격, `1e36` 과학 표기와 NaN/Infinity 방어를 자동 검증. Godot 4.7.1 headless exit 0, Primary `godot` MCP validate 3/3. |
| S2-G5 Merge 표시 통합 | Presentation | VERIFIED | `ball_merged(result_level, world_position)` 1회당 read-only pixel debris/value FX 1회를 HUD 내부 EffectManager가 생성한다. Primary `godot` validate 7/7, Desktop Main runtime에서 Lv0 pair→Lv1 active ball 1개·FX 1개·score state 불변과 큰 값 format을 확인했다. 2026-08-12 MCP 종료 후 clean Web release export를 실제 in-app browser에서 로드해 1280×720 Canvas 활성/focus, Merge 진행에 따른 genealogy 공개, console warning/error 0을 확인했다. |
| S3-G1 Stage 데이터 | Content/Systems | VERIFIED | 2026-08-12 Stage resource와 content verification을 Stage당 5종 계약으로 재정렬했다: Ground `[0,1,2,3,4]`, Planetary `[4,5,6,8,10]`, Galactic `[10,11,12,13,14]`. 세 Stage의 Time Bonus는 `[0,0.25,0.5,1,2]`, Galactic만 `black_hole_enabled=true`다. 최근 팀 검토로 Lv7 `Red Giant`와 Lv9 `Nebula`를 15종 catalog에는 보존하되 기본 Run에서 제외하는 배치가 의도된 계약임을 재확인했다. 2026-08-21 사용자 확정으로 Ground `clear_score`는 `4e8`(Moon 4개 Cashout), Planetary는 `4e25`(Galaxy 4개 Cashout), Galactic은 `0`이다. Stage HUD 게이지와 Score Clear는 같은 StageDefinition 값을 read-only로 소비한다. Godot 4.7.1 CLI에서 갱신한 catalog verification과 Planetary Score Clear를 거치는 3-Stage 통합 검증이 각각 통과했고, Primary `godot` MCP scene validation 2/2도 통과했다. |
| S3-G2 Stage 진입·Cashout 점수/시간 | Core | VERIFIED | 2026-08-12 `StageRuntime.enter_stage()`가 stage score/time만 data-defined 값으로 초기화하고 run score는 보존한다. `apply_active_cashout(amount, global_level)`는 current Stage의 `local_ball_levels`에서 비연속 global ID를 조회해 Time Bonus를 한 번 반영한다. Godot 4.7.1 CLI headless scene exit 0, Primary `godot` validate 5/5, runtime 호출에서 Ground Lv2 `+0.5s`, Planetary Lv8(local Lv3) `+1.0s`, stage/run `25/35`, score/time signal 각 5회를 확인했고 Main runtime error 0. |
| S3-G3 Tick 종료 중재 | Core | VERIFIED | 2026-08-21 `StageRuntime.get_valid_play_delta()`가 `min(delta, max(stage_time_left, 0))`만 허용하고, StageManager는 그 유효 구간만 simulation과 tick 중재에 전달한다. 따라서 `0.03s`가 남은 `0.1s` callback은 `0.03s`까지만 Active Cashout/Time Bonus를 commit하며, deadline 뒤 이동은 다음 gameplay tick으로 넘기지 않는다. core verification은 pre-deadline Lv3 Cashout의 시간 회복, local Lv4 비종료와 Time Up lock을 확인했다. Godot 4.7.1 CLI headless S3-G2/G3/G4/G5 및 S5-G3/G5/G6 회귀 exit 0, Primary `godot` validate 7/7, Main runtime error 0, MCP 종료 뒤 clean Web release export와 실제 Browser gameplay/console warning·error 0을 확인했다. |
| S3-G4 Snapshot Settlement | Core | VERIFIED | 2026-08-12 `SettlementService.settle(snapshot)`은 snapshot의 `global_level`만 사용해 BallCatalog base score를 합산하고, cashout modifier·전달된 score는 무시한다. `settlement_applied`가 재호출과 signal 중복을 막고 stage reset에서만 풀린다. Godot 4.7.1 CLI headless S3-G4/G3/G2 exit 0, Primary `godot` validate 6/6, runtime에서 Lv0+Lv1+Lv4 = `100000101`, stage/run `100000111`, duplicate `0`, lifecycle signal 각 1회를 확인했고 Main runtime error 0. |
| S3-G5 Clear·Fail 상태 통합 | Integration | VERIFIED | 2026-08-21 StageManager가 callback의 유효 gameplay 구간만 Simulation에 step하고 그 구간의 Cashout만 StageRuntime으로 전달하도록 연결했다. 통합 verification은 `0.03s` 전 하단 crossing의 Lv3 Cashout이 `+1.0s`로 PLAYING을 유지하는 경우와, deadline 뒤 crossing은 Active Cashout이 아니라 Final Settlement base score `1,000,000`만 반영하고 `TIME_UP_LOCKED→SETTLING→FAILED`가 되는 경우를 각각 확인한다. Retry/기존 상태 전이 회귀도 유지했다. Godot 4.7.1 CLI headless S3-G2/G3/G4/G5 및 S5-G3/G5/G6 회귀 exit 0, Primary `godot` validate 7/7, Main runtime error 0, clean Web release export의 Browser gameplay/console warning·error 0을 확인했다. |
| S3-G6 Stage HUD | Presentation | VERIFIED | 2026-08-12 Stage name/time/stage score/run score/clear target과 현재 Stage의 고정 5칸 세로 genealogy를 HUD에 연결했다. Stage entry는 base ball만 공개하고, `ball_merged(result_level, world_position)`의 새 local level만 한 번 공개한다. HUD는 score/time/state를 변경하지 않는다. Godot 4.7.1 CLI headless S3-G6/S1-G4 verification exit 0, Primary `godot` validate 5/5와 Main runtime에서 Ground readout·Snowflake/Snowball/Big Snowball reveal·runtime error 0을 확인했다. MCP 종료 뒤 clean Web release export를 In-app Browser에서 열어 Canvas, live HUD/genealogy, console warning/error 0을 확인했다. |
| S4-G1 Spatial Grid | Core | VERIFIED | 2026-08-21 기존 level-aware Merge grid와 1,000공 후보 감소 Evidence를 유지한 채, 서로 다른 level 및 최고공 pair를 모든 level bucket의 swept 인접 cell에서 찾는 non-Merge contact path로 확장했다. Godot 4.7.1 headless fixture에 오류가 없었고 Primary `godot` validation 5/5를 통과했다. |
| S4-G2 슬롯 재사용·할당 점검 | Core | VERIFIED | 2026-08-12 SoA free slot 우선 재사용을 256공 deactivate→respawn에서 capacity 256 유지로 확인했다. Spatial Grid는 level/cell bucket 배열을 pool로 유지하고 stable occupancy 반복 120회에서 `grid_new_buckets=0`·bucket capacity 불변을 확인했다. Merge pair, consumed flag, cashout, render snapshot buffer를 hot path에서 재사용하며 `simulation_metrics_updated(metrics)` read-only signal을 제공한다. Primary `godot` validate 10/10, Main runtime에서 active 300·per-ball child Node 0·기존 S2 후보/merge 동일·runtime error 0. Godot 4.7.1 native headless S4-G2/G1 및 S2-G2/G3·S1-G1/G3 회귀 6개 모두 exit 0. |
| S4-G3 Web release·1,000공 stretch 스트레스 | Core | VERIFIED | 2026-08-12 필수 Gate를 실제 Web 500개 최저 30 FPS 이상으로 재설정하고, 1,000개는 stretch/torture로 유지했다. 당시 clean Web 500 Merge OFF는 평균 `52.1`/최저 `38.6 FPS`, physics `2.18ms`, console warning/error 0으로 필수 Gate를 통과했다. S4-G4 구현 전 audit에서 기존 stress scene의 Merge ON 분기가 공을 생성하지 않는 문제가 발견돼, 기존 Merge ON stretch 수치는 현행 비교 근거로 사용하지 않는다. Merge ON을 실제 생성하도록 고친 최신 측정은 S4-G4 Evidence에 기록한다. |
| S4-G4 일반 Snowball MultiMesh renderer | Core | IMPLEMENTED | 기존 global-level별 reusable MultiMesh batch에 active logical Play Field shader clip uniform을 추가했다. renderer fixture는 clip rect binding과 기존 batch/reset/Black Hole fallback을 확인했고 Primary runtime은 Main title→Ground 진입 및 shader runtime error 0을 확인했다. Cashout 중 하단 배경 누수의 실제 clean Web release 재현·부재 증거는 아직 없다. |
| S5-G1 Stage 콘텐츠 매핑 | Content/Systems | VERIFIED | 2026-08-12 기존 Resource가 최신 S5 계약과 이미 일치해 값은 변경하지 않고 독립 검증을 추가했다. Ground `[0,1,2,3,4]`, Planetary `[4,5,6,8,10]`, Galactic `[10,11,12,13,14]`는 각 5종이며 이전 top이 다음 base다. spawn `6/15/35`, neutral `visual_radius_scale=1`, background `ground/planetary/galactic`가 Resource 데이터로 제공된다. Lv7·Lv9는 15종 BallCatalog에 존재하지만 기본 chain에서 비활성이고 Lv14는 `Black Hole`/`black_hole`이다. Godot 4.7.1 native headless verification exit 0, Primary `godot` validate 4/4와 Main runtime data query 성공, runtime error 0. |
| S5-G2 Stage re-baselining runtime | Core | VERIFIED | 2026-08-12 `apply_stage_definition(definition)`과 read-only stage snapshot을 StageRuntime/Simulation에 제공한다. 적용 시 simulation 배열과 Stage score/time/end lock을 초기화하고 run score는 보존한다. Merge 결과는 current ordered chain의 다음 ID를 사용해 Planetary `6→8→10`을 생성하며 top/out-of-chain 입력은 거부한다. Planetary base/spawn `4/15`, Galactic `10/35`, Lv10 local radius `64→4` 재기준화를 확인했다. Godot 4.7.1 native headless S5-G2 및 S2-G3/S3-G2~G4/S4-G1~G2 회귀 7개 exit 0. Primary Main runtime Ground base `0`, spawn `6`, radius `4`, PLAYING, runtime error 0. |
| S5-G3 Scale Shift 상태 통합 | Integration | VERIFIED | 2026-08-12 `StageManager`가 settlement 뒤 `CLEARED→SHIFTING`만 허용하고 `stage_shift_started(next_definition, shift_id)`를 발행한다. matching `shift_id`의 완료 API만 Planetary 진입을 허용하며 wrong/duplicate 완료는 무시한다. S5-G4 전 Main의 `auto_complete_shift_presentation` adapter가 deferred 한 번 같은 API를 호출한다. Godot 4.7.1 CLI project load exit 0, Primary validate StageManager/Main/S5-G3 test 4/4, S5-G3·S3-G5 integration scene은 각각 exit 0, Primary Main runtime에서 Ground top-ball→`SHIFTING`/active 0/pending id 1→Planetary `PLAYING`/pending -1과 runtime error 0을 확인했다. |
| S5-G4 Stage World·Shift presentation | Presentation | VERIFIED | 2026-08-14 v2 Paper-8 프레임과 Ground/Planetary/Galactic Stage World 3종을 연결했다. 배경은 `1600×900`, 승인 palette, opaque, 2×2 grid 검증 3/3 통과; Ground 48 snow, Planetary 28 twinkle, Galactic 44 twinkle overlay를 사용한다. 0.9s Shift에서 배경 crossfade와 중앙 베젤·200px 양쪽 wing·HUD/Pause가 함께 이동하고 completion ID를 한 번 반환한다. Godot 4.7.1 CLI Stage World/Shift, frame bounds, HUD/Pause 회귀와 Native OpenGL Ground/Shift 캡처가 통과했다. Cashout 방향 cue와 CRT emission/static은 향후 개선으로 문서화했다. |
| S5-G4I Shift presentation handoff wiring | Integration | VERIFIED | 2026-08-14 Main 임시 auto-complete adapter를 끄고 `stage_shift_started(next_definition, shift_id)`→Presentation→`stage_shift_presentation_finished(shift_id)`→StageManager accept를 연결했다. CLI에서 완료 전 Ground/SHIFTING 유지, 완료 뒤 Planetary/PLAYING 1회 진입, stale 완료 거부와 S5-G3 회귀를 확인했다. lock 해제. |
| S5-G5 3-Stage 통합 완주 | Integration | VERIFIED | 2026-08-14 Main 연속 경로에서 Ground Top Ball→Planetary, Planetary Supernova 4회 Cashout(`+4.00s`)→Time Up Score Clear→Galactic, Stage score reset/run score preserve, 2회 Shift, Retry→Ground를 CLI로 확인했다. Debug build 강제 검증은 `F6` Top Ball Clear, `F7` Time Up Score Clear다. Godot 4.7.1 CLI 회귀 6종과 Native OpenGL Main smoke exit 0, Web Debug/Release export 및 로컬 HTTP 주요 파일 200 통과. 사용자 Chrome 수동 Web 검증에서 Canvas focus 뒤 F6/F7 Clear와 Stage 전환을 실제 확인했고 Console에 red error는 없었다. 노란 `WebGL INVALID_OPERATION` 경고와 user gesture 전 `AudioContext` 경고는 기능을 막지 않았으며 S6/S9 Web 품질 점검 대상으로 기록한다. |
| S6-G1 이벤트 등급·FX budget | Content/Systems | VERIFIED | 2026-08-15 `EffectManager`에 BallDefinition tier 기반 Merge/Cashout 공통 budget, tier별 active `12/8/4/2/1`, frame spawn `4/4/3/2/1`, 전체 active 24, 낮은 FX eviction과 Transition/Terminal 예약 API를 구현했다. Godot 4.7.1 CLI 자동 scene exit 0: Tier 0 20건 중 4건 표시·16건 throttle, 같은 burst Tier 3/Cashout 보존, 24개 포화 시 오래된 하위 tier 1건 교체, 삭제 대기 FX slot 반환, 잘못된 tier 조회 거부, Clear→Settlement→Shift 순서, Terminal dominance, Terminal 뒤 Retry와 진행 중 Pause Retry의 FX·Run 단위 counter reset을 확인했다. Primary validation 8/8. S2-G3/S4-G4/S3-G5/S5-G3/S5-G5 CLI 회귀와 Main headless 120 frames exit 0. 실제 Main Galactic 395 active balls에서 공·패들·Tier 3 FX 가독성과 budget을 확인했고, 별도 Ground runtime에서 viewport 아래 Cashout source가 Y `804`에 표시되어 하단 cabinet과 분리되는 것을 screenshot으로 확인했다. 각 MCP runtime error 0. background MCP 순간 21 FPS는 release 성능 수치가 아니며 Web burst 성능은 S6 Slice 후속 Gate다. 기존 S2-G5 scene은 StageDefinition 미적용으로 stale assertion 4건이 남아 별도 Presentation test repair가 필요하다. |
| S6-G2 FIRST CONTACT CUT-IN·화면 연출 | Presentation | PENDING | 공통 `1600×900` 배경 1개와 6종 title/portrait layer 초안은 준비됐다. runtime controller는 미구현이며 S3-G9 Core producer와 S6-G2I pause/S8 handoff가 각각 실제 Evidence로 `VERIFIED`되기 전에는 시작하지 않는다. Presentation input은 Integration이 pause 수락 뒤 전달한 payload v1, output은 matching `first_contact_cutin_finished(event_id, run_epoch)`뿐이다. |
| S6-G2I FIRST CONTACT CUT-IN pause·S8 handoff | Integration | VERIFIED | 2026-08-21 GameManager가 run epoch·schema/roster-valid FIFO를 중재하고, 수락된 CUT-IN 전 Stage timer/simulation/spawn/Paddle을 lock한다. matching normal finish만 PLAYING을 재개하며 stale/wrong/duplicate finish와 terminal arbitration은 거부한다. 첫 Black Hole은 matching CUT-IN finish 전 Phase ID를 발급하지 않고, 완료 뒤 기존 S8 phase presentation flow로 넘긴다. Retry/Main/fresh Run은 epoch·queue·lock을 초기화한다. Godot 4.7.1 CLI/headless project load exit 0, Primary `godot` S6-G2I fixture exit 0(`fifo=true pause=true stale_rejected=true black_hole_gate=true reset=true`) 및 S8-G4 downstream regression exit 0. Presentation runtime controller는 다음 S6-G2 범위다. |
| S6-G3 Audio 콘텐츠 | Content/Systems | VERIFIED | 2026-08-15 `resources/audio/audio_catalog.tres`에 파일명과 동일한 22개 event key를 OGG AudioStream으로 1:1 등록했다. Godot 4.7.1 CLI headless verification exit 0: `S6_G3_VERIFIED audio_events=22 ogg_streams=22`; 키 중복·누락, key/filename 불일치, null stream, non-OGG source, loop 설정을 검증했다. S6-G1 FX budget regression exit 0 및 Main headless 120 frames exit 0. 실제 재생·priority/polyphony·첫 입력 audio unlock·Browser playback은 S6-G4 범위다. |
| S6-G4 사운드 계층·가독성 | Content/Systems/Release 담당 | VERIFIED | 2026-08-15 AudioManager에 catalog 기반 재생과 현재 공개된 read-only event 구독을 구현했다. Merge T0~T3은 priority `10/20/30/45`, polyphony `2/2/1/1`, cooldown `0.10/0.14/0.20/0.28s`, volume `-12/-10/-8/-6dB`; Cashout은 `25/50`, `2/1`, `0.14/0.22s`, `-7/-4dB`; 상태음은 `70~100`, UI는 `40`/1/`0.08s`를 사용한다. pool 포화 시 더 높은 priority만 낮은 non-terminal sound를 선점하고, terminal은 낮은 priority playback을 정리한다. Core/Integration 공식 경로로 Main에 mount하고, `SettlementService → StageManager → GameManager → AudioManager`를 통해 settlement start/finish를 매핑한다. Settlement finish는 start clip이 끝난 뒤 재생한다. Pause는 Core의 공유 toggle request를 따르며, 두 번째 요청은 Resume 처리와 함께 무음이다. `main_menu_requested`는 `ui_menu`, `settings_requested`는 `ui_click`, `start_requested`는 `ui_start`, `stage_state_changed(RUN_ENDED)`는 `run_end`로 매핑한다. Primary Godot validation 8/8, S1-G5 verification exit 0, S6-G4 verification exit 0으로 cooldown, settlement 순차 재생, transition gameplay suppression, terminal preemption, loop stop, retry, 공유 pause toggle, menu/settings/start/run-end 매핑을 확인했다. 2026-08-18 갱신 후 관련 script와 Main/test scene validation 5/5, S6-G4 verification exit 0, 실제 Main에서 UI mapping 활성 key 각 1개와 runtime error 0을 확인했다. Native Main 500공에서 공·패들·HUD와 6개 동시 policy sound를 확인했다. 실제 Chrome Web 실행에서 Canvas 렌더링과 console error 0을 확인했고, 사용자가 첫 입력 뒤 Pause 음을 청취해 Web audio unlock·재생을 확인했다. 500·1,000볼 Web 스트레스 Scene은 console error 없이 렌더링했다. 2026-08-18 실제 Main Web late-density probe에서 simulation tick을 유지한 500공, spatial candidates `2291`, grid cells `432`, 5초 시점 `60 FPS`를 측정했고 Paddle·공·HUD 경계를 시각 확인했다. 첫 Canvas 입력 뒤 일반/중요음 6개가 `dropped=0`으로 동시에 재생됐고 `run_end`가 6개를 선점해 단독 재생됐다. WebGL2 single-threaded build의 console warning/error는 0건이었다. 임시 probe를 제거하고 clean Web build 재-export·Main smoke를 통과했으며, 최종 clean 저장소의 Godot 4.7.1 CLI/headless S6-G4 verification scene과 Main 120-frame smoke도 모두 exit 0이었다. Goal을 `VERIFIED`로 닫는다. |
| S6-G5 BGM 상태 전환 | Content/Systems/Release 담당 | VERIFIED | 2026-08-19 `AudioCatalog`에 6개 OGG BGM 키를 등록하고, 별도 `AudioStreamPlayer` music channel을 구현했다. 실제 Main runtime에서 첫 입력 unlock 뒤 `bgm_planetary` 재생, Pause의 `bgm_pause` 교체 및 19.97초 위치를 저장한 Resume, Black Hole Phase의 BGM 정지+기존 loop 단독 재생, finale의 loop 정리+`bgm_result` 재생을 확인했다. S6-G3/G4/G5 자동 씬은 모두 exit 0, 관련 script/scene validate 5/5를 통과했다. 새 BGM 포함 Godot 4.7.1 Web release export와 HTML/JS/WASM/PCK HTTP 200은 통과했다. 2026-08-20 Chrome Web에서 사용자가 Pause BGM 전환·Resume 위치 재개와 실제 완주 중 `bgm_galactic` → `black_hole_loop` → `bgm_result` 전환을 모두 청감 확인했다. 같은 날 Result→Main에서 `bgm_result`가 남는 회귀를 StageManager `READY` state의 `bgm_title` 전환으로 수정하고 headless 회귀를 통과했다. |
| S3-G7 local Lv4 비종료·즉시 Score Clear 계약 | Core | VERIFIED | 2026-08-21 deadline-bounded tick 회귀에서 local Lv4 discovery는 자체 종료 사유가 아니며, 유효 구간 내 Cashout을 commit한 뒤 남은 시간이 없을 때만 `TIME_UP`을 한 번 요청함을 확인했다. deadline 밖 이동·Cashout은 현재 tick에 commit하지 않으므로 local Lv4도 뒤늦은 gameplay outcome을 만들지 않는다. Godot 4.7.1 CLI headless S3-G3/G5 및 관련 Stage/Scale Shift 회귀 exit 0, Primary `godot` validate 7/7, Main runtime error 0, clean Web Browser gameplay/console warning·error 0을 확인했다. 이 증거는 전용 discovery producer, six-identity seen set, `event_id` 또는 `run_epoch`가 존재함을 증명하지 않으며 해당 범위는 S3-G9다. |
| S3-G8 Stage Score gauge | Presentation | VERIFIED | 2026-08-20 우측 CRT의 canonical 20-cell gauge가 authoritative `stage_score / clear_score`를 read-only로 표시한다. 0점은 빈 화면, 70%는 아래 14칸, overflow는 20칸으로 clamp하고 점수 감소·Stage reset을 반영하며 Galactic(`clear_score <= 0`)에서는 숨긴다. S6-G6 Settlement 중에는 Stage Score label과 같은 presentation count-up 값을 사용한다. 기존 CLI/Native 0%·63%·100%·Galactic hidden 검증과 120-frame 평균 `60.8 FPS`, 최저 `51.6 FPS`, 최대 frame `19.40ms` evidence를 유지한다. |
| S3-G9 Run-scoped FIRST_CONTACT discovery producer | Core | VERIFIED | 2026-08-21 `BallSimulationManager`의 실제 deterministic Merge commit 바로 뒤에 payload v1 producer를 추가했다. Integration은 같은 instance에서 `begin_first_contact_run(run_epoch)`/`invalidate_first_contact_run(run_epoch)`를 호출하고 `first_contact_discovered(payload)`를 구독한다. seen set은 `apply_stage_definition()`/runtime reset을 넘어 current Run에 남고, event ID는 process lifetime에서 증가한다. dedicated headless scene은 정확한 6 identity, `ball_merged` 선행, schema/type/roster/position, Ground→Planetary→Galactic preserve, Moon local Lv0 direct spawn·out-of-chain·duplicate 거부, invalidation/Main no emit, fresh epoch 재발견, same-tick deterministic event order, first Black Hole ordinal `1`/`BLACK_HOLE_PHASE` readiness를 검증했다. Godot 4.7.1 headless exit 0; S2-G3 Merge와 S3-G2 StageRuntime/S3-G3 tick/S3-G4 settlement regressions도 exit 0. GameManager/StageManager/Main lifecycle wiring과 visible CUT-IN/Phase deferral는 의도적으로 S6-G2I 범위다. |
| S5-G6 Stage Clear 축하 확인 UI | Presentation | IMPLEMENTED | 2026-08-21 self-contained Paper8 CRT Panel이 copied read-only `outcome=CLEARED` snapshot+`clear_id`로 완료 Stage, Stage Score, Run Score, 실제 `NEXT STAGE` Button만 표시한다. Godot 4.7.1 CLI `S5_G6_VERIFIED open=true scores=true focus=true request_once=true duplicate_hidden_stale=true reset=true exclusions=true reduced=true core_readonly=true`; S1-G4/S3-G6/S3-G8/S5-G4/S5-G7/S6-G6 회귀 모두 exit 0. Native OpenGL Compatibility Intel Arc 130V 캡처는 120 frames 평균 `60.1 FPS`, 최대 frame `19.56ms`, PNG save error 0. Main 120-frame headless smoke exit 0이며 기존 shutdown-only ObjectDB 3/resource 1 warning은 유지된다. Main mount/StageManager·GameManager wiring과 실제 Browser path는 S5-G6I 전이라 미검증이므로 `VERIFIED`가 아니다. |
| S5-G6I Next Stage 확인 wiring | Integration | VERIFIED | 2026-08-21 `StageManager`가 non-final Score Clear 뒤 monotonic `clear_id`와 copied snapshot을 한 번 발행하고, matching NEXT STAGE request 전까지 `CLEARED`에 고정한다. Main의 StageClearPanel request만 matching ID를 수락해 기존 distinct `shift_id` presentation flow를 시작하며 wrong/duplicate request는 거부한다. Retry/Main은 pending panel/state를 초기화하되 numeric IDs는 재사용하지 않는다. 같은 wiring에서 non-final Time Up 및 Black Hole score-depletion failure도 terminal ResultPanel로 전달하도록 복구했다. Godot 4.7.1 CLI editor/headless project load exit 0과 S3-G5 verification exit 0, Primary `godot` runtime에서 Ground Score Clear Panel→NEXT STAGE→Planetary PLAYING, Planetary Time Up→ResultPanel, score 0 Black Hole run-end→ResultPanel을 screenshot/runtime state로 확인했다. MCP 종료 뒤 clean Web release export를 localhost browser에서 로드해 `1280×720` Canvas와 focus 뒤 F7 입력 전달, console warning/error 0을 확인했다. |
| S5-G7 Galactic 투명 Stage World | Presentation | VERIFIED | 2026-08-20 Galactic runtime plate를 완전 투명 plate와 stars/galaxy/nebula alpha layer로 교체하고 Ground/Planetary opaque 경로를 유지했다. CLI `S5_G7_VERIFIED`, Native L2/L3/Reduced 캡처, Chrome 151 WebGL2 Main startup·Canvas focus·입력·console warning/error 0, disposable capture의 normal `60.5/57.8 FPS`와 reduced `60.4/57.5 FPS` evidence를 확인했다. |
| S7-G1C Item Ball·Orb producer contract | Content/Systems | VERIFIED | 2026-08-21 `ItemManager`가 Stage당 1회 spawn, 현재 Stage `local_level >= 2`의 분리된 5-hit, Orb 수직 낙하·Paddle pickup/열린 하단 miss의 상호 배타적 1회 signal을 제공한다. `ItemDefinition` 3종과 StringName signal 계약을 추가했으며 activation/Core state 변경은 S7-G1에 남겼다. Primary `godot` validate 6/6과 runtime verification `S7_G1C_VERIFIED item_ball=once hits=5 orb=collect_or_miss`, exit 0을 확인했다. 이 환경에는 Godot CLI 실행 파일이 없어 PATH/표준 설치 경로 조회만 가능했고, CLI baseline은 tooling issue로 남긴다. |
| S7-G1 Item gateway 통합 | Integration | IMPLEMENTED | 2026-08-21 ItemManager를 Main에 mount하고 Orb 획득→event_id CUT-IN request→matching activation cue/explicit skip→1회 effect request Gateway를 연결했다. duplicate/stale cue와 Retry/Main reset을 차단한다. 2026-08-21 Integration이 mounted Blizzard consumer까지 연결했다: matching Blizzard activation만 `ItemBlizzard.activate(item_blizzard.tres)`로 전달하며 Spawn loop는 Stage base rate와 runtime multiplier를 분리해 ×3/1.0을 한 번 적용·복구한다. Gateway skip fallback, Stage base-rate 변경 중 active multiplier 보존, expiry·Retry·Main reset을 S7-G1 integration fixture에서 확인했다. 실제 Presentation CUT-IN 화면 producer가 아직 없어 Orb 획득→visible CUT-IN→cue의 사용자 경로와 Q-S7 Web smoke는 미검증이다. |
| S7-G2 Blizzard | Content/Systems | IMPLEMENTED | 2026-08-21 Content-owned `ItemBlizzard` runtime이 `item_blizzard.tres`의 5초·×3 값을 수락하고, 재획득은 지속시간만 갱신해 multiplier를 누적하지 않는다. 만료/Reset은 normal `1.0` 복구 요청을 정확히 한 번 발행한다. Primary `godot` validate에서 script·verification script·scene 3/3 valid, verification scene은 exit 0(`S7_G2_IMPLEMENTED duration=5 spawn_multiplier=3 refresh=non_stacking restore=once`). 2026-08-21 Integration mount/consumer가 `spawn_multiplier_changed(multiplier)`를 GameManager의 base Stage spawn rate에 연결해 Ground `6→18`, Planetary active-effect `15→45`, expiry `45→15`, Retry/Main `→6`을 자동 검증했다. 실제 Orb CUT-IN과 Web smoke는 S6-G2 Presentation producer 뒤에 검증한다. |
| S7-G3 Fire Core | Content/Systems | PENDING | S7-G1/S3 회귀 필요; 선택 |
| S7-G4 Magnet | Content/Systems | PENDING | S7-G1/S4 metric 필요; 선택 |
| S8-G1 Black Hole force | Core | VERIFIED | 2026-08-17 흡수 패널티를 Cashout 가치 `12.5%`와 첫 Black Hole 등장 시점 Run Score `25%` 중 작은 값으로 재조정했다. phase baseline은 한 번만 snapshot하며 단일 저등급 흡수는 자금이 있는 Run을 즉시 끝내지 않고, 고가 공 반복 손실은 고정 cap으로 Run Score를 0까지 소진할 수 있다. 인력 첫 플레이테스트 seed는 반경 `300`, source 최대 `450`, 다중 source 합산 cap `900`, mutual pull `450`으로 조정했다. 기존 Lv14→최대 2개 Black Hole entity 전환, phase request, 하단 반사/Cashout·일반 Merge 제외 계약은 유지한다. 최신 Primary 검증 결과는 이번 변경 Worklog에 기록한다. L2→L3 실제 연결·최종 충돌은 S8-G4/S8-G2 범위. |
| S8-G2 두 Black Hole 최종 충돌 runtime | Core | VERIFIED | 2026-08-14 Black Hole relative sweep의 earliest contact를 한 번 lock하고 이후 normal ball simulation commit을 멈춘다. simulation은 `black_hole_finale_started(contact_snapshot)`, StageRuntime은 contact·두 Black Hole state와 stage/run score를 포함한 immutable-style `black_hole_finale_locked(result_snapshot)`을 제공한다. 첫/두 번째 Lv14 생성만으로는 끝나지 않으며 terminal은 Top Ball/Stage Shift를 요청하지 않는다. Godot 4.7.1 headless S8-G2 및 S8-G1/S2-G3/S3-G2 회귀 exit 0, Primary `godot` validate 4/4와 Main runtime Ground/PLAYING/error 0. UI·actual finale wiring은 S8-G3/S8-G4 범위. |
| S8-G3 Title·Main·Terminal UI | Content/Systems/Release 담당 | VERIFIED | 2026-08-21 사용자가 Godot Desktop과 Chrome Web 양쪽에서 finale 이후 Result가 표시되고, Result의 `RETRY RUN`으로 fresh Ground Run이 재시작되며 `MAIN`으로 Title UI에 복귀하는 전체 경로를 실제 확인했다. 기존 자동 검증의 read-only result snapshot, 누락 통계 행 숨김, 실제 Button, hover/press, request 1회 계약과 함께 Desktop/Web 수동 검증 Gate를 충족해 Goal을 닫는다. Title의 독립 랜덤 게이지·연동 전등·중앙 창 한정 눈, Result의 화면 아래→위 진입·실험관 기포·독립 게이지 motion과 `CLEAR SCORE`/`TOTAL MERGES`/`RUN TIME` 표시를 확정 산출물로 유지한다. |
| S8-G4 Black Hole Finale·Retry 통합 | Integration | VERIFIED | 2026-08-21 사용자 실제 플레이에서 첫 Black Hole Snowball→phase 진입·기믹 생성→두 번째 Black Hole 생성→두 기믹 충돌→finale→Result를 확인했고, L2→L3 뒤 Galactic 재개 및 Result `RETRY RUN` Ground 초기화·`MAIN` Title 복귀도 확인했다. 기존 Godot 4.7.1 CLI/headless S8-G4 Integration·S8-G2 회귀와 Web export evidence를 합쳐 terminal snapshot 1회 전달, stale/duplicate phase 방어, retry/main reset gate를 충족했다. 이 Evidence는 `black_hole_phase_started` 이후 downstream을 계속 증명하지만 FIRST_CONTACT 완료 전 Phase 금지를 증명하지 않으며, 그 upstream gate는 PENDING S6-G2I 범위다. |
| S8-G5 Black Hole Phase presentation | Presentation | VERIFIED | 2026-08-21 실제 Desktop/Web 플레이에서 첫 Black Hole 뒤 L2 `880`→L3 `1040` Frame/HUD 확장·Galactic 재개와 두 번째 Black Hole 충돌 뒤 finale→Result를 확인했다. active scene은 authoritative snapshot을 그리는 단일 `BlackHolePhaseEffect`와 no-argument `black_hole_finale_presentation_finished()` handoff를 사용한다. Presentation의 run-generation reset, duplicate/stale phase 방어와 reduced-effects 경로를 유지하며 기존 CLI phase/finale·S5-G4 회귀 및 Native 60-frame 평균 `61.3 FPS`, 최저 `54.9 FPS`, 최대 frame `18.21ms` evidence를 함께 유지한다. |
| S9-G1 Release tuning·telemetry | Content/Systems/Release 담당 | VERIFIED | 2026-08-21 Primary validation 5/5 및 runner runtime 통과. 자연 플레이 2 Run×3 Stage에서 timer/bonus/dwell equation, Cashout local level 분포, active/candidate/grid peak를 기록했다. Ground는 Time Up→Settlement Clear 1회와 즉시 Score Clear 1회, Planetary는 즉시 Score Clear 2회로 자연 Shift했으며, 최고 active 156/candidate 115/grid 154로 관측됐다. 근거상 현재 StageDefinition과 no-time-cap을 유지한다. 사용자 결정에 따라 S6-G2 CUT-IN과 선택 S7은 RC-1 범위에서 제외하며, 최종 합류 뒤 Web QA를 재수행한다. |
| S9-G2 Web export·browser QA | Content/Systems/Release 담당 | VERIFIED | RC-1은 S6-G2 CUT-IN과 S7 Optional Items를 제외한다. 기준은 HEAD `602ce25`와 2026-08-21 현재 작업 트리다. MCP 종료 뒤 fresh temporary Web export(HTML/JS/WASM/PCK)와 static load/console smoke를 확인했고, Main·S9 telemetry·3-stage integration scene·Paddle은 Primary validate 4/4 valid다. 사용자 Chrome의 `127.0.0.1:8080`에서 실제 Canvas 렌더링, Resume, A·← 입력, Pause, modal Retry로 fresh Ground reset, 1024×768 resize, audible audio, console error 0을 확인했다. 사용자 실제 late-game 플레이에서도 눈에 띄는 버벅임이 없음을 확인했다. CLI scene 실행은 전역 `user://logs` 접근 실패 뒤 signal 11로 종료되어 tooling issue로 분리한다. |
| S9-G3 Public link·submission | Content/Systems/Release 담당 | VERIFIED | 2026-08-21 itch.io 공개 URL `https://kosh1668.itch.io/snowball-effect`에서 설명·커버가 정상 표시됨을 확인했다. 새 시크릿 창에서 직접 접속해 캐시 없는 Canvas load, Start, 키보드 입력, 한 판 완주, Result, Retry를 실제 플레이로 확인했다. S9-G2의 Web console error 0 evidence를 유지하며 공개 배포 Gate를 충족했다. |

## 현재 다음 행동

- S0 Bootstrap의 세 Goal이 모두 `VERIFIED`됐다.
- S1-G1/G3/G4/G5는 기존 `VERIFIED`를 유지한다.
- S1-G2는 Mouse/Keyboard arbitration과 large-ball direct-sweep overlap 회귀까지 `VERIFIED`다.
- S1-G6은 최신 clean Web export의 사용자 Chrome 수동 검증까지 완료해 `VERIFIED`다. S1 Shared Skeleton이 닫혔다.
- S2-G1은 최신 catalog 계약에 맞춰 재정렬·재검증되어 `VERIFIED`다. Lv6 Sun/Lv7 Red Giant/Lv9 Nebula/Lv10 Galaxy와 Lv14 Black Hole Resource를 유지한다.
- S2-G2/G3은 모든 non-Merge pair의 물리 contact로 구현·재검증해 다시 `VERIFIED`다. `ball_merged(result_level, world_position)` 2인자와 비연속 Stage progression(`6→8→10`)은 유지한다.
- S2-G5의 기존 Merge 표시 Web Evidence를 유지하며 S2 전체는 다시 닫혔다.
- S3-G1~G7은 최신 Stage data, deadline-bounded runtime, settlement, Main flow와 HUD까지 모두 `VERIFIED`다.
- S4-G1은 모든 non-Merge pair contact broad phase를 구현·재검증해 다시 `VERIFIED`다. S4-G4는 active Play Field clip을 구현했으나 실제 clean Web release 확인 전 `IMPLEMENTED`다. S4-G2/G3의 slot·allocation·Web 성능 Evidence는 유지하고, 기존 MultiMesh와 500/1,000 Merge ON 수치도 보존한다.
- S5-G1은 기존 Stage/Ball Resource mapping을 전용 검증으로 재확인해 `VERIFIED`다. 다음 순차 Core Goal은 S5-G2 Stage re-baselining runtime이며, Presentation S5-G4도 S5-G1 background key와 기존 Shift signal 계약을 바탕으로 작업 가능하다.
- S5-G1~G5와 S5-G4I가 모두 `VERIFIED`다. 사용자 Chrome Web에서 Canvas focus 뒤 Debug `F6`/`F7` Clear와 Stage 전환을 확인했다. red console error는 없었고, WebGL/AudioContext warning은 S6/S9 품질 점검 대상으로 남긴다.
- S8-G1 Black Hole force가 `VERIFIED`다. Core는 Black Hole runtime entity/force/흡수/점수차감과 phase·run-end request API를 제공하며, S8-G4가 L2→L3 logical rect 활성화와 실제 재개 wiring을 완료했다.
- S8-G2 Black Hole terminal runtime이 `VERIFIED`다. Core가 earliest contact 이후 simulation commit을 잠그고 finale/result snapshot을 제공하며, S8-G4가 S8-G3 Result UI와 S8-G5 Presentation producer 연결 및 Retry/Main reset을 완료했다.
- S8-G3/G4/G5는 실제 Desktop/Web 완주 관찰과 자동 회귀를 합쳐 모두 `VERIFIED`다. active scene은 latest Main의 단일 Black Hole overlay, phase ID handoff, 외부 no-argument `black_hole_finale_presentation_finished()`를 사용하고 Presentation은 run-generation·duplicate/stale·reduced-effects 방어를 유지한다.
- S8-G4의 기존 phase/finale/Retry API와 Evidence는 유지한다. 첫 Black Hole `FIRST_CONTACT` 완료 전에 Phase를 시작하지 않는 upstream gate는 새 S6-G2I가 구현·검증해야 하며, 이를 과거 S8-G4 Evidence로 소급 완료 처리하지 않는다.
- S6-G1·G3·G4·G5는 Content/Systems가 소유하고, S6-G2 FIRST CONTACT visual은 Presentation이 소유한다. S6-G5는 별도 music channel의 6개 BGM, Pause/Resume 위치 복원, Black Hole loop 전환을 실제 Chrome Web 완주 청감까지 확인해 `VERIFIED`다.
- S6-G1과 S6-G3, S3-G9, S6-G2I는 `VERIFIED`다. Presentation S6-G2는 `priority_event_reserved`와 준비된 6종 layer를 사용해 runtime controller를 시작할 수 있다.
- S6-G2I가 run epoch lifecycle, FIFO CUT-IN pause와 Black Hole handoff를 구현·검증했고 Integration lock은 released다. 다음 순서는 Presentation `S6-G2`이며, 실제 CUT-IN 화면 producer가 연결될 때까지 Main은 gameplay를 멈추지 않는다.
- 2026-08-20 사용자 청감 피드백에 따라 S6-G4의 `scale_shift` 효과음을 `-1 dB`에서 `-7 dB`로 낮췄다. priority `85`와 나머지 재생 정책은 유지했으며 Godot 4.7.1 CLI/headless S6-G4 verification exit 0, Primary validation 3/3을 통과해 `VERIFIED` 상태를 유지한다.
- 2026-08-20 S5-G4I visual Backdrop sync maintenance를 완료했다. Presentation의 `visual_field_rect_changed(Rect2)`가 Scale Shift와 Black Hole L2→L3 Frame 보간값을 Integration GameManager에 전달해 장식용 검은 Backdrop만 같은 rect로 갱신한다. Simulation·Paddle·Cashout logical rect는 전환 완료 전까지 기존 값을 유지한다. Godot 4.7.1 CLI integration verification과 S8-G5 presentation regression은 exit 0, Primary runtime의 active tween에서 Backdrop/Frame inner width가 모두 `719.776`이고 logical width는 `560`으로 유지됨을 확인했으며 clean Web release export도 `[ DONE ] savepack`으로 통과했다.
- 2026-08-20 사용자 청감 피드백에 따라 S6-G4의 전체 SFX playback에 공통 `-6 dB` master attenuation을 적용했다. policy별 상대 크기·priority·polyphony·cooldown과 BGM channel `-14 dB`는 유지한다. Godot 4.7.1 CLI S6-G4 verification과 S6-G5 BGM regression이 각각 exit 0, Primary validate 3/3, clean Web release export `[ DONE ] savepack`을 확인했다.
- 2026-08-21 `fx-design`과 latest Main 통합에서 S3-G8의 canonical 20-cell CRT gauge, 당시 S5-G6I automatic Clear→Settlement→Shift, S5-G7 transparent Galactic world, S6-G6 read-only settlement count-up, S8-G4/G5 Black Hole handoff를 하나의 active scene wiring으로 재검증했다. Godot 4.7.1 CLI/headless S3-G7, S3-G8, S5-G4 stage world, S5-G5, 당시 S5-G6I, S5-G7, S6-G6, S8-G4, S8-G5 두 검증이 모두 exit 0이었다. 이 automatic evidence는 최신 확인 대기 계약으로 superseded됐으며, S5-G6I는 새 wiring 전까지 `PENDING`이다. Main headless와 Native OpenGL Compatibility smoke, Web release export도 당시 exit 0이다. 기존 S5-G4 playable fixture의 Pause B 위치/Paddle 경계, obsolete `TOP_BALL_CLEAR` fixture, Pause B 이전 asset-count fixture는 latest Main에서도 재현되는 stale assertion으로 별도 정리가 필요하다. 브라우저 smoke는 `browse` Windows 서버 번들이 없어 실행하지 못했다.
- 2026-08-21 S5-G6 Presentation producer를 `IMPLEMENTED`로 재활성화했다. 현재 Main은 의도적으로 Panel을 mount하지 않아 automatic Shift 구현을 유지하며, 최신 계약을 실제 runtime에 여는 다음 Goal은 `PENDING` S5-G6I다. S5-G6I는 `stage_clear_ready(snapshot, clear_id)` 연결, matching first request 수락, 별도 `shift_id` 발급, Retry/Main/new Run reset과 Desktop/Web 3-Stage 회귀를 담당한다.
- 알려진 별도 문제: 기존 Web preset의 `export_filter="all_resources"`가 `build/` 산출물까지 다시 패킹한다. S1-G1 범위 밖이므로 수정하지 않음.
